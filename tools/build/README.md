# Build Tools

This directory contains build automation scripts for the OpenLinuxManager project.

## Usage

Add build scripts here to automate common build tasks, optimizations, and CI/CD pipeline helpers.
